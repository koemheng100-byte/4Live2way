import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.koemheng.livetranslator',
  appName: 'KH Live Translator',
  webDir: 'dist'
};

export default config;
